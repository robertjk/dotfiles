# Modules reference: https://zimfw.sh/docs/modules/

# Tab-completion
zmodule completion

# Set generic Zsh built-in environment options
# HISTFILE set in .zshrc
zmodule environment

# Correct keybindings for input events
zmodule input

# Run help for the current command on ESC-h
zmodule run-help

# `archive` and `unarchive` functions
zmodule archive

# Convenient way to load ssh-agent
zstyle ':zim:ssh' ids 'id_ed25519'
zmodule ssh

# Utility aliases; adds color to `ls`, `grep` and `less`
zmodule utility

# Aliases and better defaults for exa
zmodule exa

# FZF
zmodule fzf

# Starship prompt
zmodule joke/zim-starship

# Reminds you to use existing aliases
zmodule MichaelAquilina/zsh-you-should-use
